# 정산 인터페이스 재설계 초안

상태: 설계 검토용. 구현 및 운영 반영 금지. 2026-09-13.
진행 요청: 아스트라에서 설계 확정 후 솔에서 구현. 모델 전환은 앱에서 수행한다.
이 문서는 현재 작업의 인계 초안이며 특정 모델로 검토가 완료됐다는 의미가 아니다.

## 목표와 범위

기존 폼의 색상 변경이 아니라 정보 구조와 입력 동선을 재설계한다.
화이트 기반, 검정 타이포그래피, 절제된 강조색을 사용한다.
기존 기능 목록과 계정별 계산 규칙을 먼저 확정하고 화면을 새로 구성한다.
운영 앱과 데이터는 유지한다. 이전 preview 버전은 폐기하고 재사용하지 않는다.

## 화면 구조

## 시각 디자인 명세 · 1차 검토안

방향: 흰색 업무 노트. 큰 숫자, 명확한 정렬, 얇은 구분선으로 구성하고
버튼과 입력 영역만 형태를 부여한다. 화면 전체를 카드로 쪼개지 않는다.

| 요소 | 결정안 |
| --- | --- |
| 배경 | 본문 #FFFFFF, 보조 영역 #F5F6F7 |
| 글자 | 주요 #202124, 보조 #62676F |
| 강조 | 작업 버튼 #202124, 선택·포커스 #2457D6, 저장 완료 #23734B |
| 구분선 | #E4E7EB, 1px |
| 글꼴 | Pretendard 우선, 시스템 sans-serif 대체; 숫자 tabular-nums |
| 글자 크기 | 본문 15px, 보조 13px, 화면 제목 28px, 대표 금액 모바일 36px·PC 44px |
| 간격 | 4/8/12/16/24/32px 단위; 모바일 좌우 20px, PC 본문 32px |
| 컨트롤 | 최소 터치 영역 44px, 반경 6px; 숫자 오른쪽 정렬 |
| 아이콘 | 일관된 선형 아이콘; 이모지 제거; 아이콘 버튼에 접근성 이름·도움말 |

### PC 배치

전체 너비를 활용하되 본문 최대 1280px. 왼쪽 메뉴 184px, 본문과 보조 열 비율 2:1.
상단은 화면 제목과 날짜·계정, 본문 왼쪽은 금액 입력과 품목 행,
오른쪽은 오늘 합계와 추가한 인센티브 내역, 하단은 최종 저장으로 구성한다.
상세 내역은 입력 영역 옆에서 확인하므로 긴 폼 끝까지 스크롤할 필요가 없다.

정산에서는 왼쪽에 수령액·지급/공제 명세, 오른쪽에 선택한 항목의 편집기를 표시한다.
편집 전에는 오른쪽에 정산 기준을 보여준다. 일별 표는 별도 상세 보기로 전환한다.

### 모바일 배치

기록 화면의 첫 화면: 월간 입력 달력 → 선택 날짜 → 오늘 합계·저장 상태 → 금액 입력·추가 → 최근 추가 내역.
그 아래에 품목 행 7개와 해당 계정의 시간수당 입력을 배치한다.
저장 바는 스크롤 컨테이너 하단에 sticky로 배치하고 본문 하단에 같은 높이 여백을 둔다.
키보드가 열리면 하단 내비게이션이 입력을 가리지 않아야 하며, 안 되면 저장 바를 일반 흐름으로 전환한다.

정산 화면의 첫 화면: 월·기간 → 수령액 → 지급 합계/공제 합계 → 상세 명세.
수정 항목을 선택하면 해당 행 아래에 편집기를 펼친다. 이전 화면 아래에 새 화면을 덧붙이지 않는다.
관리자 편집은 별도 화면에서 직원 선택 → 적용 기준 → 변경 전후 비교 → 저장 순서로 진행한다.

### 상단 입력 현황 달력

목적은 날짜 선택보다 누락 확인이다. 기록 화면 제목 바로 아래에 현재 월 달력을 항상 보이게 둔다.
PC에서는 입력 영역 너비에 맞는 한 달 달력을 표시하고, 모바일에서는 좌우 20px 안에서 7열을 유지한다.

| 상태 | 표시 |
| --- | --- |
| 정상 입력 완료 | 날짜 아래 작은 초록 점 |
| 휴무 입력 완료 | 날짜 아래 회색 가로 표시 |
| 오늘, 미입력 | 옅은 주황 테두리와 작은 `미입력` 표시 |
| 지난 날짜, 미입력 | 빈 원 표시 |
| 미래 날짜 | 흐린 숫자, 선택 불가 |
| 선택 날짜 | 검정 원 또는 검정 밑줄 |

달력 상단에는 `2026년 9월`, 이전·다음 달 이동, `오늘` 버튼을 둔다.
날짜를 누르면 해당 날짜 입력값과 저장 상태를 아래 작업 영역에 불러온다.
월 이동은 달력 상태만 바꾸고 정산 화면의 선택 월에는 영향을 주지 않는다.
주말을 자동 휴무로 판단하지 않으며, 저장된 `휴무` 기록만 휴무로 표시한다.
미입력 표시는 확인용이며 급여 계산에는 반영하지 않는다.

### 아이폰 급여 보고 화면

모바일 정산 화면 상단에 `보고용 보기` 버튼을 둔다. 누르면 편집 버튼·하단 메뉴·다운로드를 숨기고
정산 기간, 직원명, 예상 수령액, 지급 내역, 공제 내역, 최종 계산 기준을 한 열로 재배치한다.
보고용 화면은 iPhone Safari의 CSS 픽셀 너비 375px와 390px에서 우선 검수한다.

항목 수에 따라 높이가 달라지므로 Safari의 `전체 페이지` 캡처를 기본으로 한다.
일반 화면 캡처에서도 수령액과 지급·공제 합계는 첫 화면에 들어오게 한다.
계정 ID·관리 기능·비밀번호·설정값은 보고 화면에서 숨긴다.
금액은 줄바꿈하지 않고 tabular-nums를 사용하며, 375px에서도 긴 금액이 잘리지 않아야 한다.

### 하단 메뉴 구현 기준

Streamlit 기본 내비게이션에는 모바일 하단 탭 위치가 없다. 구현은 가능하며 두 방식이 있다.

1. 현재 화면 전환용 `st.radio`를 키가 있는 컨테이너에 두고 CSS로 하단 고정한다.
2. Streamlit 커스텀 컴포넌트로 하단 메뉴를 만든다.

1차 구현은 의존성이 없는 첫 번째 방식을 사용한다. iPhone 홈 인디케이터 영역에는
`env(safe-area-inset-bottom)`을 반영하고, 본문에는 메뉴 높이만큼 하단 여백을 둔다.
숫자 키보드가 열리면 메뉴를 숨기거나 일반 흐름으로 돌려 입력칸과 저장 버튼을 가리지 않게 한다.
각 화면은 조건부로 하나만 렌더링해 월 변경 후 이전 화면 아래에 새 화면이 이어지는 현상을 방지한다.
실제 iPhone Safari에서 위치·키보드·재실행이 불안정할 때만 커스텀 컴포넌트로 교체한다.

### 주요 상태와 동작

- 금액 입력 후 추가는 임시 내역에 누적한다. 저장 버튼이 서버 저장의 유일한 확정 동작임을 표시한다.
- 직전 취소는 마지막 추가만 되돌린다. 리셋은 입력 중인 인센티브만 초기화한다.
- 저장 중에는 중복 제출을 막고, 성공 후에만 저장됨으로 바꾼다. 실패하면 입력과 재시도 버튼을 유지한다.
- 계정 변경·날짜 변경·로그아웃 시 미저장 내용이 있으면 저장/버리기/계속 작성 선택을 제공한다.
- 휴무·인센없음·삭제는 날짜 메뉴에 배치한다. 실적을 덮어쓰거나 삭제하는 경우 대상 날짜와 변경을 확인한다.
- 설정 로드 중에는 빈 자리 표시를 쓰고, 오류에는 실제 금액처럼 보이는 기본값을 채우지 않는다.
- 애니메이션은 선택·열기 상태에 120~160ms 정도만 사용한다. 저장이나 월 전환을 꾸미기 위한 지연은 넣지 않는다.

### 구현 가능성 확인 항목

위 내용은 목표 디자인이며 Streamlit에서 검증된 구현물이 아니다.
표준 위젯으로 금액 입력·목록·조건부 편집을 먼저 구성한다.
하단 내비게이션, sticky 저장 바, 입력칸 포커스 유지, 키보드 대응은
별도 테스트에서 확인하고 불안정하면 일반 배치로 대체한다.
디자인 목적으로 계산식이나 계정 권한을 바꾸지 않는다.

아이폰 검수는 375px·390px 폭에서 달력 7열, 긴 직원명, 긴 금액, 공제 항목 최대 개수,
Safari 전체 페이지 캡처를 확인한다. 하단 메뉴는 일반 스크롤, 숫자 키보드, 화면 회전,
브라우저 뒤로가기와 Streamlit 재실행 후에도 현재 화면을 유지해야 한다.

### 시안 판단 기준

기존과 달라져야 하는 핵심은 PC 2열 작업 공간, 모바일 입력 우선 배치,
최근 기록·월간 표의 분리, 해당 항목 옆에서 수정하는 정산 흐름이다.
색과 반경만 바뀌었다면 이 설계의 완료로 인정하지 않는다.

## 화면 목록

| 화면 | 첫 화면에서 보여줄 정보 | 주요 조작 | 보조 정보 |
| --- | --- | --- | --- |
| 기록 | 선택 날짜, 저장 상태, 오늘 합계 | 인센티브 연속 추가, 취소, 품목 수량, 저장 | 최근 기록, 삭제 |
| 정산 | 정산 월·기간, 예상 수령액, 지급·공제 합계 | 월 변경, 지급·공제 상세 수정 | 일별 명세, 엑셀 다운로드 |
| 내 정보 | 본인에게 적용되는 급여·품목·수당 기준 | 비밀번호 변경, 로그아웃 | 정산 기준 설명 |
| 관리자 | 직원 선택, 선택 직원의 적용 기준 | 급여·품목·단가·정산일·전체적용 수정, 비밀번호 초기화 | 변경 전후 비교 |

모바일은 기록·정산·내 정보 하단 내비게이션, 관리자는 내 정보에서 진입한다.
PC는 좌측 내비게이션과 넓은 본문을 사용한다. 모바일 폼을 단순 확대하지 않는다.
사용 가능한 높이를 확인해 하단 내비게이션이 키보드나 저장 버튼을 가리지 않게 한다.

## 기록 화면

1. 상단에 날짜 이동·달력, 근무/휴무 상태, 저장 여부를 한 영역으로 묶는다.
2. 오늘 합계와 인센티브 누적 내역을 먼저 보여준다. 합계에는 입력 중/저장됨을 구별한다.
3. 금액 입력 → 추가 → 입력칸 비우기·포커스 유지 흐름을 제공한다. 직전 취소와 전체 초기화를 구분한다.
4. 품목명·단가·수량을 행으로 묶는다. 계정에 설정된 모든 품목을 빠짐없이 노출한다.
5. 시간수당 대상 계정에만 퇴근 시간과 계산된 시간수당을 표시한다.
6. 최종 저장 버튼과 저장 상태는 입력 영역 가까이 배치한다. 저장하지 않은 변경은 날짜 이동 전에 처리한다.
7. 휴무 선택으로 기존 유효 실적을 지울 때만 변경 내용을 확인한다. 실패하면 입력을 유지한다.
8. 최근 7일 기록은 날짜 선택 영역에서 열도록 옮긴다. 월간 리포트는 정산 화면에 집중한다.

PC에서는 입력을 왼쪽, 해당 날짜 누적 내역·합계를 오른쪽에 둔다.
모바일에서는 동일 기능을 한 열로 배치하고 입력·저장을 우선한다.

## 정산 화면

1. 월 선택과 정확한 정산 시작·종료일을 상단에 고정된 순서로 표시한다.
2. 예상 수령액 다음에 기본급·실적·시간수당·기타 지급과 보험·현금·카드·기타 공제를 구분한다.
3. 각 수정 가능한 항목을 누르면 그 항목의 편집 영역을 연다. 공제 폼 전체를 항상 펼치지 않는다.
4. 카드 사용액·제외액·실공제액을 함께 보여주고, 제외 내역의 이월 여부를 명시한다.
5. 일별 내역은 별도 상세 영역에서 본다. 모바일 요약 행에서 품목 상세를 열고 PC는 표를 사용한다.
6. 엑셀은 화면과 같은 기간·기준·합계에서 생성한다.

## 데이터 계약과 계정별 차이

현재 코드에서 확인한 차이:

- 태완: 관리자 메뉴 및 시간수당.
- 남근: 시간수당, 본인 정보 조회.
- 나머지 코드 정의 계정: 본인 정보 조회, 시간수당 입력 비노출.
- 계정별 기본급, 보험료, 정산일, 품목명 7개와 단가 7개, 시간수당 단가, 과거 단가 전체 적용 여부.
- 설정이 없는 경우 다른 직원의 설정을 복사하는 기존 동작도 존재한다.

이 목록은 코드 기준이며 실제 운영 시트의 최신 설정과 일치하는지는 미확인이다.
화면 재설계 중 계정별 값을 추정하거나 기본값으로 대체하지 않는다.
설정 로드 실패는 오류로 표시하고 저장을 막는다. 0원이나 기본급으로 조용히 대체하지 않는다.
운영 설정을 자동 생성·보정하는 기존 읽기 함수로 검증 데이터를 가져오지 않는다.
운영 자료는 읽기 전용으로 확보하고, 별도 테스트 저장소로 복사하기 전 대상과 차이를 확인한다.
가상 데이터가 필요하면 별도의 가상 직원명만 사용한다. 실제 직원명에 샘플 금액을 붙이지 않는다.

## 구현 전 확정할 검증 기준

- 각 계정별 동일 입력·동일 기간에 대한 기존/신규 기본급, 실적, 시간수당, 공제, 실수령액 대조표.
- 합계 차이가 있으면 계산 오류인지 정책 차이인지 먼저 판정한다. 디자인 작업으로 계산 정책을 변경하지 않는다.
- 1일·13일·29~31일 정산, 윤년·연말, 빈 달, 과거 단가 적용, 카드 제외 이월과 마지막 제외 항목 삭제.
- 계정 변경 시 이전 계정의 날짜·미저장 입력·단가·공제가 남지 않아야 한다.
- 저장·수정·삭제·비밀번호·관리자 설정·엑셀의 성공 및 실패 흐름.

## 속도와 구현 순서

1. 최초 진입, 계정 설정 조회, 월 전환, 저장을 같은 데이터와 실행 환경에서 측정한다.
2. 운영 화면과 계산·데이터 처리의 경계를 확인한 뒤, 검증된 로직을 재사용한다.
3. 화면별 필요한 데이터만 읽고 계정·기간별 캐시를 사용한다. 저장 후 해당 데이터만 갱신한다.
4. 금액·품목 조작마다 원격 시트를 조회하지 않는다. 입력은 묶어 저장하며 불필요한 대기 시간을 제거한다.
5. 별도 테스트 버전에서 한 화면씩 구현하고 검수한다. 운영 배포는 별도 승인 후 진행한다.

로컬 가상 DB 속도를 운영 Google Sheets 성능 개선으로 보고하지 않는다.
Streamlit에서 요구 동선 구현이 어려운 부분만 확인하고, 프레임워크 교체를 미리 결정하지 않는다.

## 토큰과 작업 통제

설계 단계에서는 이 문서와 간결한 화면 배치안만 검토한다. 이미지 시안을 반복 생성하거나 코드를 작성하지 않는다.
설계 승인 후 솔에서 단독 구현한다. 서브에이전트는 사용하지 않는다.
매번 전체 파일과 대화를 다시 읽지 않고 변경 대상과 이 문서를 기준으로 이어간다.
테스트는 기능·계산 경계에 집중하며, 통과한 테스트는 관련 변경이 있을 때 다시 실행한다.
